<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/white_medium_transparency">

        <ImageView
            android:id="@+id/stat_increase_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:src="@drawable/add_image_vector"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/stat_increase_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            app:layout_constraintTop_toTopOf="@id/stat_increase_image"
            app:layout_constraintBottom_toBottomOf="@id/stat_increase_image"
            app:layout_constraintStart_toEndOf="@id/stat_increase_image"
            android:layout_marginStart="12dp"
            tools:text="Agility"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>